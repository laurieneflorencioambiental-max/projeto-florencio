'use server';

import type { Lead } from '@/lib/types';

// As funções de IA foram removidas temporariamente para resolver um conflito de dependência que bloqueava a publicação.
// Elas podem ser readicionadas posteriormente, assim que o problema de dependência for resolvido.
